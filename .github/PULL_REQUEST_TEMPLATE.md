## What

<!-- What does this PR change, and why? Link the issue if there is one. -->

## How to verify

<!-- Steps to see the change working (or which tests cover it). -->

## Checklist

- [ ] `pytest` passes (run from `server/`)
- [ ] `ruff check server/` passes
- [ ] Server + extension platform patterns kept in sync (if patterns changed)
- [ ] Migration file added under `server/migrations/` (if schema changed) — these are applied manually
